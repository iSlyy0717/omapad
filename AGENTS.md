## Agent skills

### Issue tracker

Issues live in Linear: Victor's Personal team (`VL`), project Omapad. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical triage labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Internal skills

`/forge`, `/stamp`, `/smash`, `/initiative`, and `/improve` read this repo's tracker.
Run `/unslop` on human-facing prose. Pass a path, or no args for the whole repo.
Install with `npx skills add QuietEngines/skills -g`. See `docs/agents/issue-tracker.md`.
